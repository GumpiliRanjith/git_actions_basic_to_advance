name: automate

on: workflow_dispatch

jobs:
  number_1:
    runs-on: ubuntu-22.04

    steps:
      - name: install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nano
          
