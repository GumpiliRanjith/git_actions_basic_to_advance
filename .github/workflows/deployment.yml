name: ranjith devops

on: pull_request

jobs:
  orange:
    runs-on: ubuntu-latest
    steps:
      - name: easy
        run: echo "hello world"
      
  mango:
    needs: orange
    runs-on: ubuntu-latest
    steps:
      - name: moderate
        run: echo "moderate is running"
